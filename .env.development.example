# Copie para .env.local se usar WAMP/Apache em vez do PHP embutido (:8000).
# Por padrão, npm run dev já faz proxy /api → http://localhost:8000/api

# VITE_WAMP_API_TARGET=http://127.0.0.1
# VITE_WAMP_API_PREFIX=/consorcio/api
